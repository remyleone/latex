

\ProvidesPackage{themestage}[15/06/2010 v1.0 Stage style]

\RequirePackage{graphicx}
\usepackage{xcolor}

%% change the equation number appearance
%% load up the AMS math package
\RequirePackage{amsmath}

%% load up the fancy box making package
\RequirePackage{fancybox}

%% make the equation numbers have little ovals around them
\def\tagform@#1{\maketag@@@{\cornersize{0.8}\ovalbox{\ignorespaces\sffamily{#1}\unskip\@@italiccorr}}}



%% Load up PS insertion package
\RequirePackage{psboxit}
\PScommands
\definecolor{bleu1}{rgb}{0.14,0.70,0.66}
\definecolor{seccol}{rgb}{0.5,0.5,0.5}

\usepackage[top=1.4in, bottom=1.5cm, left=2cm, right=1cm]{geometry}
%% Load up fancy headers and footers package
\RequirePackage{fancyhdr}

%% now reset the headers and footers
\fancyhf{}

%% odd side pages
\fancyfoot[RO]{\sffamily\bfseries\color{white} \rightmark \hspace{1cm}\fcolorbox{white}{white}{\color{seccol}{\bfseries \thepage}}}



%% make the bottom line above the page number box
\renewcommand{\footrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

%% bring the style into effect
\pagestyle{fancy}

%% now redefine the plain style pages (chapter pages, contents pages)
%% to have the same page number stuff on the bottom
\fancypagestyle{plain}{
\fancyhf{}
\fancyfoot[RO]{\sffamily\bfseries\color{white} \rightmark \hspace{1cm}\fcolorbox{white}{white}{\color{seccol}{\bfseries \thepage}}}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
}

%% this next section (till \makeatother) makes sure that blank pages
%% are actually completely blank, cause they're not usually
\makeatletter
\def\cleardoublepage{\clearpage\if@twoside \ifodd\c@page\else
	\hbox{}
	\vspace*{\fill}
	\thispagestyle{empty}
	\newpage
	\if@twocolumn\hbox{}\newpage\fi\fi\fi}
\makeatother

%% now change the chapter style
%% load up the quotchap package
\RequirePackage[avantgarde]{quotchap}

%% make the quotation appear next to the chapter number
\renewcommand\chapterheadstartvskip{\vspace*{-5\baselineskip}}

%% now change the section heading to have a line beneath it
%% load up the fancy title-style package

\RequirePackage[calcwidth]{titlesec}
\RequirePackage{color}
\usepackage{fix-cm}

\titleformat{\section}[hang]{\sffamily\bfseries}{
\fontsize{100}{100}\selectfont\color{seccol}\thesection}{0pt}{\linebreak\huge\raggedleft}[{\titlerule[0.5pt]}]
\renewcommand*\thesection{\arabic{section}}


\titleformat{\chapter}[hang]{\sffamily\bfseries}{}{0pt}{\fontsize{48}{48}\selectfont\center}[{}]

\usepackage{eso-pic}

\newcommand{\eleve}[1]{#1}
\newcommand{\titrestage}[1]{#1}

\newcommand{\maitrestage}[1]{#1}
\newcommand{\maitrestagefonction}[1]{#1}
\newcommand{\debutstage}[1]{#1}
\newcommand{\finstage}[1]{#1}
\newcommand{\boite}[1]{#1}
\newcommand{\logoboite}[1]{#1}

\newcommand\IIETitle{
\begin{titlepage}
\begin{center}
% background titre
\AddToShipoutPicture*{\includegraphics{img/firstbackground150}}


\sffamily\bfseries\large{Minist\`ere de l'\'Education Nationale\\}
\vspace{0.75cm}
\sffamily\bfseries\large{\'Ecole Nationale Sup\'erieure d'Informatique pour l'Industrie et l'Entreprise\\}
\vspace{1.5cm}
\sffamily\bfseries\LARGE{RAPPORT de STAGE\\}
\vspace{2.5cm}
\sffamily\bfseries\Huge{\titrestage\\}
\vspace{4cm}
\sffamily\bfseries\large{\eleve \\Stage de 2\`eme ann\'ee\\}
\vspace{2cm}
\sffamily\bfseries\large{Ma\^itre de stage : \maitrestage\\ \maitrestagefonction \\}
\vspace{2cm}
\sffamily\bfseries\normalsize{Stage effectu\'e \`a : \boite \\du \debutstage \ au \finstage \\}
\vspace{2cm}
\begin{tabular}{lcr}
\includegraphics[width=7cm]{img/logo_ensiie} & & \includegraphics[width=7cm]{\logoboite}\\
\end{tabular}

\pagebreak
\end{center}
\end{titlepage}
}

%%
%% End of file `phdthesis.sty`
